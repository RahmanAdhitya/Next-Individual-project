import { content_types } from '../types';

const init_state = {
  id: 0,
  image: '',
  location: '',
  caption: '',
  likes: '',
};

export const content_reducer = (state = init_state, action) => {};
